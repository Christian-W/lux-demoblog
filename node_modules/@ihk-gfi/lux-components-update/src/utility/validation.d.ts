import { SchematicContext, Tree } from "@angular-devkit/schematics";
export declare function validateNodeVersion(context: SchematicContext, minimumVersion: string): void;
/**
 * Prüft die Angular Version der aufrufenden Applikation und wirft eine SchematicsException, wenn
 * die Version nicht der erforderlichen entspricht.
 * @param tree
 * @param context
 * @param angularVersion
 */
export declare function validateAngularVersion(tree: Tree, context: SchematicContext, angularVersion: string): void;
/**
 * Prüft die LUX-Components Version der aufrufenden Applikation und wirft eine SchematicsException, wenn
 * die Version nicht der erforderlichen entspricht.
 * @param tree
 * @param context
 * @param minimumVersion
 */
export declare function validateIhkGfiLuxComponentsVersion(tree: Tree, context: SchematicContext, minimumVersion: string): void;
/**
 * Prüft die LUX-Components Version der aufrufenden Applikation und wirft eine SchematicsException, wenn
 * die Version nicht der erforderlichen entspricht.
 * @param tree
 * @param context
 * @param minimumVersion
 */
export declare function validateLuxComponentsVersion(tree: Tree, context: SchematicContext, minimumVersion: string): void;
