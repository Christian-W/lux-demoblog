import { UnitTestTree } from '@angular-devkit/schematics/testing';
import { Schema as WorkspaceOptions } from '@schematics/angular/workspace/schema';
import { Schema as ApplicationOptions } from '@schematics/angular/application/schema';
/**
 * Standard-Optionen für das Test-Workspace.
 */
export declare const workspaceOptions: WorkspaceOptions;
/**
 * Standard-Optionen für die Test-Applikation.
 */
export declare const appOptions: ApplicationOptions;
/**
 * Fügt eine Dependency in die erste Zeile der Property "dependencies" aus der package.json.
 * @param appTree
 * @param dependency
 * @param version
 */
export declare const addDependencyToPackageJson: (appTree: UnitTestTree, dependency: string, version: string) => void;
/**
 * Erzeugt einen Mock-Context für den Aufruf über callRule.
 */
export declare const createMockContext: () => import("@angular-devkit/schematics").TypedSchematicContext<{}, {}>;
