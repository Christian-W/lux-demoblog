import { OnDestroy } from '@angular/core';
import { LuxConsoleService } from './lux-console.service';
import { Observable } from 'rxjs';
import { BreakpointObserver } from '@angular/cdk/layout';
export declare class LuxMediaQueryObserverService implements OnDestroy {
    private breakpointObserver;
    private logger;
    private _mediaQueryChanged;
    private _subscriptions;
    constructor(breakpointObserver: BreakpointObserver, logger: LuxConsoleService);
    readonly activeMediaQuery: string;
    ngOnDestroy(): void;
    getMediaQueryChangedAsObservable(): Observable<string>;
    isXS(): boolean;
    isSM(): boolean;
    isMD(): boolean;
    isLG(): boolean;
    isXL(): boolean;
    private addQuerySubscription;
}
