import { Overlay } from '@angular/cdk/overlay';
import { Injector } from '@angular/core';
import { LuxFilePreviewConfig } from './lux-file-preview-config';
import { LuxFilePreviewRef } from './lux-file-preview-ref';
export declare const DEFAULT_FILE_PREVIEW_CONFIG: LuxFilePreviewConfig;
export declare class LuxFilePreviewService {
    private injector;
    private overlay;
    constructor(injector: Injector, overlay: Overlay);
    open(config: LuxFilePreviewConfig): LuxFilePreviewRef;
    private attachDialogContainer;
    private createOverlay;
    private createInjector;
    private getOverlayConfig;
}
