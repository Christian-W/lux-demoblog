import { EventEmitter } from '@angular/core';
/**
 * Base-Klasse der LuxActionComponents.
 *
 * Enthält die Inputs/Outputs, die allen Action-Components gleich sind.
 */
export declare class LuxActionComponentBaseClass {
    luxLabel: string;
    luxColor: 'primary' | 'accent' | 'warn' | '';
    luxRaised: boolean;
    luxIconName: string;
    luxTagId: string;
    luxDisabled: boolean;
    luxRounded: boolean;
    luxIconAlignWithLabel: boolean;
    luxClicked: EventEmitter<any>;
}
