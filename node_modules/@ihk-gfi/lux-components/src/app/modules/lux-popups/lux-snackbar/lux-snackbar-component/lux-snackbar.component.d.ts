import { OnInit } from '@angular/core';
import { MatSnackBarRef } from '@angular/material';
import { LuxSnackbarConfig } from '../lux-snackbar-config';
import { Observable } from 'rxjs';
export declare class LuxSnackbarComponent implements OnInit {
    config: LuxSnackbarConfig;
    snackbarRef: MatSnackBarRef<LuxSnackbarComponent>;
    private action$;
    textFontColor: string;
    actionFontColor: string;
    iconFontColor: string;
    constructor(config: LuxSnackbarConfig, snackbarRef: MatSnackBarRef<LuxSnackbarComponent>);
    ngOnInit(): void;
    onAction(): Observable<void>;
    actionClick(): void;
    /**
     * Prüft ob die übergebene Farbe Teil des Enums ist.
     * Wenn nicht, wird standardmäßig "gray" zurückgegeben.
     * @param colorToCheck
     */
    private checkColorInEnum;
}
