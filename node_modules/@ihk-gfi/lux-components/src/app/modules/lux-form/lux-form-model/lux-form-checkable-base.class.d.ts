import { ControlContainer, ValidatorFn } from '@angular/forms';
import { ChangeDetectorRef, EventEmitter, SimpleChanges } from '@angular/core';
import { LuxConsoleService } from '../../lux-util/lux-console.service';
import { LuxFormComponentBase } from './lux-form-component-base.class';
import { LuxComponentsConfigService } from '../../lux-components-config/lux-components-config.service';
/**
 * Basis-Klasse für FormComponents, die einen ähnlichen Grundaufbau für das Aktivieren eines Boolean-Wertes besitzen
 * (LuxToggle und LuxCheckbox z.B.).
 */
export declare abstract class LuxFormCheckableBaseClass extends LuxFormComponentBase {
    luxCheckedChange: EventEmitter<boolean>;
    luxTagId: string;
    luxReadonly: boolean;
    luxChecked: boolean;
    protected constructor(controlContainer: ControlContainer, cdr: ChangeDetectorRef, logger: LuxConsoleService, config: LuxComponentsConfigService);
    notifyFormValueChanged(formValue: boolean): void;
    errorMessageModifier(value: any, errors: any): string;
    protected checkValidatorsContainRequired(validators: ValidatorFn | ValidatorFn[]): ValidatorFn | ValidatorFn[];
    protected triggerOutputPatternCheck(): void;
    protected triggerInputPatternCheck(simpleChanges: SimpleChanges): void;
}
