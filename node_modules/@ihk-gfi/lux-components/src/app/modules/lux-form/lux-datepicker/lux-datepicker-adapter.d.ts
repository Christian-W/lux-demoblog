import { NativeDateAdapter } from '@angular/material';
import DateTimeFormatOptions = Intl.DateTimeFormatOptions;
export declare class LuxDatepickerAdapter extends NativeDateAdapter {
    private readonly dotRegExp;
    private readonly backslashRegExp;
    private readonly hyphenRegExp;
    private readonly hyphenRegExp_1;
    format(date: Date | string, displayFormat: DateTimeFormatOptions): string;
    parse(value: string): Date | null;
    /**
     * UTC Date mit 0-Werten für Time aus einem ddMMYYYY-String erhalten.
     * @param dateString
     * @param separator
     */
    private getUTCNulled_ddMMYYYY;
    /**
     * UTC Date mit 0-Werten für Time aus einem YYYYMMdd-String erhalten.
     * @param dateString
     * @param separator
     */
    private getUTCNulled_YYYYMMdd;
    /**
     * UTC Date mit 0-Werten für Time aus einem MMddYYYY-String erhalten.
     * @param dateString
     * @param separator
     */
    private getUTCNulled_MMddYYY;
    isValid(date: any): boolean;
}
