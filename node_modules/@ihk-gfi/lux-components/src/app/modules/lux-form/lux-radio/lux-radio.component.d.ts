import { ChangeDetectorRef, OnDestroy, TemplateRef } from '@angular/core';
import { ControlContainer } from '@angular/forms';
import { LuxMediaQueryObserverService } from '../../lux-util/lux-media-query-observer.service';
import { LuxConsoleService } from '../../lux-util/lux-console.service';
import { LuxFormSelectableBase } from '../lux-form-model/lux-form-selectable-base.class';
import { LuxComponentsConfigService } from '../../lux-components-config/lux-components-config.service';
export declare class LuxRadioComponent extends LuxFormSelectableBase implements OnDestroy {
    private mediaObserver;
    forceVertical: boolean;
    tempRef: TemplateRef<any>;
    pb3: boolean;
    luxGroupName: string;
    luxOrientationVertical: boolean;
    private mediaSubscription$;
    constructor(controlContainer: ControlContainer, cdr: ChangeDetectorRef, logger: LuxConsoleService, config: LuxComponentsConfigService, mediaObserver: LuxMediaQueryObserverService);
    readonly isVertical: boolean;
    ngOnDestroy(): void;
}
