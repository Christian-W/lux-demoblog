import { ChangeDetectorRef, ElementRef } from '@angular/core';
import { LuxFormComponentBase } from '../lux-form-model/lux-form-component-base.class';
export declare const luxFormControlSelektor: string;
/**
 * @author dron
 * @description Diese Component wird von den anderen LUX-FormControls genutzt, um eine einheitliche Baseline zu erreichen.
 */
export declare class LuxFormControlComponent {
    private cdr;
    focused: boolean;
    _luxScalableHeight: boolean;
    _luxHideBottomBorder: boolean;
    /**
     * Die zugrunde liegende FormComponent
     */
    luxFormComponent: LuxFormComponentBase;
    luxFormComponentElementRef: ElementRef;
    luxIgnoreDefaultLabel: boolean;
    /**
     * Dient dazu, eine Component beliebig Hoch werden zu lassen (z.B. Textarea oder Radio).
     * @param scalable
     */
    luxScalableHeight: boolean;
    /**
     * Bestimmt ob die untere Border ausgeblendet werden soll oder nicht (z.B. bei Checkbox).
     * @param hide
     */
    luxHideBottomBorder: boolean;
    constructor(cdr: ChangeDetectorRef);
    /**
     * Gibt wieder, ob der Fehler für diese FormComponent dargestellt werden soll.
     */
    shouldDisplayError(): boolean;
    shouldDisplayMisc(): string | boolean | import("./lux-form-control-subcomponents/lux-form-hint.component").LuxFormHintComponent;
    shouldDisplayLabelByProperty(): string;
    shouldDisplayHintByProperty(): boolean;
    /**
     * Aktiviert den Fokus dieser Component.
     */
    focusin(): void;
    /**
     * Deaktiviert den Fokus dieser Component.
     */
    focusout(): void;
}
