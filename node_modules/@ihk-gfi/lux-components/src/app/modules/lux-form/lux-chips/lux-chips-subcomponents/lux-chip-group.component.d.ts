import { EventEmitter, OnInit, TemplateRef } from '@angular/core';
import { ThemePalette } from '@angular/material';
import { ILuxChipSelected } from '../lux-chips-model/lux-chip-selected.interface';
export declare class LuxChipGroupComponent implements OnInit {
    private _luxColor;
    tempRef: TemplateRef<any>;
    luxChipClicked: EventEmitter<number>;
    luxChipAdded: EventEmitter<string>;
    luxChipRemoved: EventEmitter<number>;
    luxChipSelected: EventEmitter<ILuxChipSelected>;
    luxLabels: string[];
    luxDisabled: boolean;
    luxRemovable: boolean;
    luxSelected: boolean;
    luxColor: ThemePalette;
    constructor();
    ngOnInit(): void;
    add(label: string): void;
    remove(index: number): void;
    click(index: number): void;
    select(selected: boolean, index: number): void;
}
