import { ChangeDetectorRef, QueryList, TemplateRef } from '@angular/core';
import { ControlContainer } from '@angular/forms';
import { LuxConsoleService } from '../../lux-util/lux-console.service';
import { LuxFormSelectableBase } from '../lux-form-model/lux-form-selectable-base.class';
import { LuxComponentsConfigService } from '../../lux-components-config/lux-components-config.service';
import { LiveAnnouncer } from '@angular/cdk/a11y';
import { MatOption } from '@angular/material';
export declare class LuxSelectComponent extends LuxFormSelectableBase {
    private liveAnnouncer;
    tempRef: TemplateRef<any>;
    matOptions: QueryList<MatOption>;
    luxPlaceholder: string;
    luxMultiple: boolean;
    luxTagId: string;
    displayedViewValue: string;
    constructor(controlContainer: ControlContainer, cdr: ChangeDetectorRef, logger: LuxConsoleService, config: LuxComponentsConfigService, liveAnnouncer: LiveAnnouncer);
    notifyFormValueChanged(formValue: any): void;
}
