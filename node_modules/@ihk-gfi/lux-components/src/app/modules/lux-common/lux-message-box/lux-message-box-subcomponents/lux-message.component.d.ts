import { EventEmitter, OnInit } from '@angular/core';
import { ILuxMessage } from '../lux-message-box-model/lux-message.interface';
export declare class LuxMessageComponent implements OnInit {
    private _luxMessage;
    backgroundCSSClass: string;
    fontCSSClass: string;
    luxMessageClosed: EventEmitter<ILuxMessage>;
    luxMessage: ILuxMessage;
    constructor();
    ngOnInit(): void;
    /**
     * Setzt die Messages auf ein leeres Array um so die MessageBox auszublenden.
     */
    close(): void;
    /**
     * Aktualisiert die Farbe dieser Box passend zur Farbe der Nachricht.
     */
    private updateColor;
}
