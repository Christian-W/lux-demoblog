export interface ILuxAppFooterButtonInfo {
    label: string;
    cmd: string;
    color?: string;
    disabled?: boolean;
    hidden?: boolean;
    raised?: boolean;
    iconName?: string;
    alwaysVisible?: boolean;
    onClick?: (that: ILuxAppFooterButtonInfo) => void;
}
export declare class LuxAppFooterButtonInfo implements ILuxAppFooterButtonInfo {
    label: string;
    color: string;
    disabled: boolean;
    cmd: string;
    hidden: boolean;
    raised: boolean;
    iconName: string;
    alwaysVisible: boolean;
    onClick: (that: ILuxAppFooterButtonInfo) => void;
    constructor(label: string, cmd: string, color?: string, disabled?: boolean, hidden?: boolean, raised?: boolean, iconName?: string, alwaysVisible?: boolean, onClick?: (that: ILuxAppFooterButtonInfo) => void);
    /**
     * Statische Methode um ein Info-Objekt zu generieren.
     * Nimmt ein Objekt vom Typ ILuxAppFooterButtonInfo entgegen.
     * @param data
     * @returns eine Button Info
     */
    static generateInfo(data: ILuxAppFooterButtonInfo): LuxAppFooterButtonInfo;
}
