import { TemplateRef } from '@angular/core';
import { Observable } from 'rxjs';
import { FormGroup } from '@angular/forms';
export declare class LuxStepComponent {
    private _iconChange;
    private _luxIconName;
    headerTemplate: TemplateRef<any>;
    contentTemplate: TemplateRef<any>;
    luxIconSize: string;
    luxOptional: boolean;
    luxEditable: boolean;
    luxCompleted: boolean;
    luxStepControl: FormGroup;
    luxIconName: string;
    getIconChangeObsv(): Observable<boolean>;
    constructor();
    /**
     * Gibt an, ob der Step als abgeschlossen gilt.
     *
     * Möglichkeiten:
     *  1. Hat ein luxStepControl, welches valid ist
     *  2. Der Wert luxCompleted ist true
     *  3. Der Wert luxOptional ist true
     */
    isCompleted(): boolean;
}
