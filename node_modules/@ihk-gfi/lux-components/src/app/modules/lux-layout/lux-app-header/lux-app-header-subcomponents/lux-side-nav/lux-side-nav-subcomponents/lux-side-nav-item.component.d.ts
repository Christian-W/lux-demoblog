import { AfterViewInit, ChangeDetectorRef, EventEmitter, OnChanges, QueryList, TemplateRef } from '@angular/core';
export declare class LuxSideNavItemComponent implements AfterViewInit, OnChanges {
    private cdr;
    templateRef: TemplateRef<any>;
    luxLabel: string;
    luxDisabled: boolean;
    luxTagId: string;
    luxSelected: boolean;
    luxCloseOnClick: boolean;
    luxIconName: string;
    luxExpandable: boolean;
    luxExpanded: boolean;
    luxClicked: EventEmitter<any>;
    sideNavItems: QueryList<LuxSideNavItemComponent>;
    constructor(cdr: ChangeDetectorRef);
    ngOnChanges(): void;
    ngAfterViewInit(): void;
    onClick($event: any): void;
}
