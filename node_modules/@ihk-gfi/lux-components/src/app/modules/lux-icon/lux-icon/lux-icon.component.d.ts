import { LuxBackgroundColorsEnum } from '../../lux-util/lux-colors.enum';
export declare class LuxIconComponent {
    static readonly FA_BRAND = "fab ";
    static readonly FA_SOLID = "fas ";
    static readonly FA_REGULAR = "far ";
    static readonly FA_LIGHT = "fal ";
    private _luxIconSize;
    private _luxIconName;
    private _luxPadding;
    private _backgroundCSSClass;
    private _fontCSSClass;
    currentIconSize: number;
    isIconFA: boolean;
    styleMargin: string;
    luxRounded: boolean;
    luxMargin: string;
    luxPadding: string;
    luxIconSize: string;
    luxIconName: string;
    readonly backgroundCSSClass: string;
    readonly fontCSSClass: string;
    luxColor: LuxBackgroundColorsEnum;
    constructor();
    /**
     * Generiert aus dem mitgegebenen Wert einen String-Wert
     * der entweder als Font-Awesome Icon oder als Material-Icon
     * interpretiert werden kann.
     *
     * @param iconName, z.B. fas fa-cogs
     * @returns string
     */
    private modifiyIconName;
}
