import { AriaDescriber, FocusMonitor } from '@angular/cdk/a11y';
import { Directionality } from '@angular/cdk/bidi';
import { Overlay } from '@angular/cdk/overlay';
import { Platform } from '@angular/cdk/platform';
import { ScrollDispatcher } from '@angular/cdk/scrolling';
import { ElementRef, NgZone, OnChanges, SimpleChanges, ViewContainerRef } from '@angular/core';
import { MatTooltip, MatTooltipDefaultOptions, TooltipPosition } from '@angular/material';
export declare class LuxTooltipDirective extends MatTooltip implements OnChanges {
    private luxOverlay;
    private luxElementRef;
    private luxScrollDispatcher;
    private luxViewContainerRef;
    private luxNgZone;
    private luxPlatform;
    private luxAriaDescriber;
    private luxFocusMonitor;
    private luxScrollStrategy;
    private luxDir;
    private luxDefaultOptions;
    luxTooltip: string;
    luxTooltipHideDelay: number;
    luxTooltipShowDelay: number;
    luxTooltipPosition: TooltipPosition;
    luxTooltipDisabled: boolean;
    constructor(luxOverlay: Overlay, luxElementRef: ElementRef, luxScrollDispatcher: ScrollDispatcher, luxViewContainerRef: ViewContainerRef, luxNgZone: NgZone, luxPlatform: Platform, luxAriaDescriber: AriaDescriber, luxFocusMonitor: FocusMonitor, luxScrollStrategy: any, luxDir: Directionality, luxDefaultOptions: MatTooltipDefaultOptions);
    _handleLongPress(): void;
    _handleKeydown(event: any): void;
    _handleTouchend(): void;
    ngOnChanges(simpleChanges: SimpleChanges): void;
}
