import { DoCheck, ElementRef, NgZone, OnChanges, Renderer2, SimpleChanges } from '@angular/core';
import { MatBadge } from '@angular/material';
import { AriaDescriber } from '@angular/cdk/a11y';
export declare class LuxBadgeNotificationDirective extends MatBadge implements OnChanges, DoCheck {
    private luxNgZone;
    private luxElementRef;
    private luxAriaDescriber;
    private luxRenderer;
    luxBadgeNotification: string;
    luxBadgeColor: 'primary' | 'warn' | 'accent' | string;
    luxBadgeSize: 'small' | 'medium' | 'large';
    luxBadgePosition: 'above after' | 'above before' | 'below before' | 'below after';
    luxBadgeDisabled: boolean;
    luxBadgeHidden: boolean;
    luxBadgeOverlap: boolean;
    luxBadgeCap: number;
    constructor(luxNgZone: NgZone, luxElementRef: ElementRef<HTMLElement>, luxAriaDescriber: AriaDescriber, luxRenderer: Renderer2);
    ngOnChanges(changes: SimpleChanges): void;
    ngDoCheck(): void;
    updateContent(value: any): void;
    isHidden(): boolean;
    /**
     * Prüft ob der Inhalt eine Zahl, eine Maximalzahl gegeben und diese überschritten worden ist.
     * Wenn ja, wird der Inhalt mit einem "+" abgekürzt.
     */
    private checkMaxNumber;
}
